How to use this on a brand new computer
Open the default terminal.

Clone this repo:
git clone https://github.com/Pyrohawk/dotfiles.git ~/dotfiles

Run the script:

Bash
cd ~/dotfiles
chmod +x install.sh
./install.sh
